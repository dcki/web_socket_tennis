:javascript
  (function() {
    var mm = App.activateMatchMakingChannel();
    var game = App.activateGameChannel();
    setInterval(function() {
      var i = Math.floor(Math.random() * 3);
      game.perform('paddle', { paddle_state: ['up', 'down', 'stop'][i] });
    }, 500);
  })();
